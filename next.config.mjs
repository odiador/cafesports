/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['1000marcas.net', 'www.uniquindio.edu.co', 'images.emojiterra.com'],
    },
};

export default nextConfig;
